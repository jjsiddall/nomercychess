// git pull or git clone
// Write the JQuery response for Destroy
// What do you want to do to the page? How will you select the element in question?
// Scan the JQuery Manipulation docs to find a method that will help

$("#move_<%= @move.id %>").remove();